<section class="kesher-section">
  <div class="kesher-container">
    <h1>{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Kesher - Story Page (Base)",
  "settings": [],
  "presets": [{ "name": "Kesher - Story Page (Base)" }]
}
{% endschema %}